name: test1 github action
on: workflow_dispatch
jobs:
  First_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "hello world!"
      - name: Print goodbye
        run: echo "Done - bye!"
